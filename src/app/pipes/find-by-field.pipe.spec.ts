import { FindByFieldPipe } from './find-by-field.pipe';

describe('FindByFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new FindByFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
